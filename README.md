# Neon Trace — Server

- Dev: `npm install && npm run dev`
- Prod: `npm start`

Environment:
- `ORS_API_KEY` (required)
- `MONGODB_URI` (optional, for logs)
- `PORT` (default 3001)

Deploy on Render:
- This repo includes `render.yaml` for easy setup. Add env vars in Render dashboard and deploy.
